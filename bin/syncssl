#!/usr/bin/env python3
import subprocess
ssl_location = "/etc/ssl/private"
ssh_hosts = []

if __name__ == "__main__":
    for host in ssh_hosts:
        proc = subprocess.Popen(["scp", "-r", ssl_location, host + ":" + ssl_location])
        proc.wait()
