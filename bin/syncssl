#!/usr/bin/env python3
import subprocess
ssl_location = "/opt/ssl_diamondbz.xyz"
ssh_hosts = []

if __name__ == "__main__":
    for host in ssh_hosts:
        print("Syncing to: " + host)
        proc = subprocess.Popen(["scp", "-r", ssl_location, host + ":" + ssl_location])
        proc.wait()
